import { PromeniTekstDirective } from './promeni-tekst.directive';

describe('PromeniTekstDirective', () => {
  it('should create an instance', () => {
    const directive = new PromeniTekstDirective();
    expect(directive).toBeTruthy();
  });
});
